# cgroup.conf is only read when TaskPlugin=task/cgroup is active.
# That plugin requires privileged containers, so we use task/affinity instead.
# This file is kept as a placeholder for Phase 2 if stronger per-job memory
# enforcement is ever worth the privileged tradeoff.
